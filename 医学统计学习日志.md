# 医学统计学习日志

日期：2026-05-12

资料根目录：`G:\第二期统计学`

系统路线文件：

- `医学统计系统学习路线_1到10.md`
- `医学统计实战项目矩阵.csv`
- `医学统计实战复盘模板.md`

## 已梳理的课程地图

1. `1R入门Rstudio`：R/RStudio、脚本组织、Quarto/R Markdown、R 包安装。
2. `2tidyverse数据清理`：数据导入、清洗、转换、tidyverse 常用语法。
3. `4统计思维`：t 检验、方差分析、重复测量、协方差分析、卡方检验、秩和检验、样本量、随机分组、相关与回归。
4. `5缺失值异常值`：异常值识别、缺失机制、缺失值处理和多重插补思路。
5. `6table1sci`：医学论文 Table 1、基线均衡、描述性统计和导出。
6. `7公共数据库`：CHARLS、CLHLS、CHNS、HRS 等公共数据库素材。
7. `9RCT`：RCT 设计、样本量、效应量、非劣效/优效、Win Ratio、统计分析计划。
8. `10真实世界`：DAG、倾向评分、匹配/加权、目标试验仿真、真实世界证据。

## 今日实战主题：真实世界研究中的 RHC 倾向评分分析

研究问题：右心导管检查（RHC）与死亡结局之间的关联，重点不是直接把观察性关联当因果结论，而是学习如何通过协变量平衡来降低混杂。

已定位资料：

- 数据：`G:\第二期统计学\10真实世界\rhc.csv`
- 教学脚本：`G:\第二期统计学\10真实世界\1-Nmatch PSM全.R`
- 已有演示结果：`G:\第二期统计学\10真实世界\演示结果\glm演示`

## 本轮掌握要点

1. 先做未调整模型：`death ~ swang1`，得到观察性粗关联。
2. 再建倾向评分模型：`swang1 ~ 基线协变量`，评估个体接受 RHC 的概率。
3. 看 PS 区分能力：AUC 高说明治疗分配可由基线特征较好预测，也提示混杂风险存在。
4. 匹配/加权后的核心诊断不是 P 值，而是 SMD。通常以 `|SMD| < 0.1` 作为平衡较好的经验阈值。
5. 效应量需要和研究设计绑定解读：粗模型、PS 调整、匹配、IPTW、重叠权重回答的目标人群不完全相同。

## Base R 复现结果

脚本：`rhc_base_psm_practice.R`

输出目录：`outputs`

关键结果：

| 分析 | OR | 95% CI | P 值 |
|---|---:|---:|---:|
| 粗 Logistic，全数据 | 1.252 | 1.118-1.402 | 9.43e-05 |
| PS 调整 Logistic | 1.297 | 1.139-1.478 | 9.26e-05 |
| 手写最近邻匹配 | 1.310 | 1.129-1.520 | 3.79e-04 |

诊断结果：

- 原始数据：5735 行；RHC 组 2184 人，No RHC 组 3551 人。
- PS 模型 AUC：0.798，提示治疗分配能被基线特征较好预测，混杂风险并不轻。
- 手写 caliper 最近邻匹配：1563 对，匹配后样本 3126 人。
- 最大 `|SMD|`：匹配前 0.501，匹配后 0.043。
- `|SMD| > 0.1` 的协变量/水平数：匹配前 45 个，匹配后 0 个。

当前解读：在这个 RHC 观察性资料中，RHC 组死亡风险更高的方向在粗模型、PS 调整和匹配后都保持一致。实战写作时不能直接说“RHC 导致死亡增加”，更合适的表述是：在可观测基线协变量平衡后，RHC 暴露仍与更高死亡风险相关；仍需讨论适应证混杂、疾病严重程度残余混杂和未测混杂。

## 下一步练习

1. 对照课程里的完整结果，理解 `MatchIt/cobalt/tableone` 相比 base R 手写流程省掉了哪些劳动。
2. 下一轮补上 Table 1：未匹配 vs 匹配后，重点解释 SMD 变化，不陷入只看 P 值。
3. 再往后进入 DAG：先画清楚“哪些变量应该调整、哪些变量不能调整”，再谈 PS。
4. 安装并熟悉 `MatchIt`、`cobalt`、`tableone` 后，复现课程中的 nearest、Mahalanobis、optimal、IPTW、overlap weighting 等完整分析链。
